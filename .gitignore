# Java / Maven
backend/target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

# Node
frontend/node_modules/
frontend/dist/
*.local

# 大数据文件（第三方词库原始数据，不进仓库；服务器上单独保留）
database/raw/

# 运行时上传文件
uploads/
backend/uploads/

# IDE
.idea/
.vscode/
*.iml

# OS
Thumbs.db
.DS_Store

# 本地敏感配置
application-local.yml
.env
.env.*

# 服务器专用部署文件（不进公开仓库，服务器本地保留）
docker-compose.server.yml
docs/DEPLOY-SERVER.md

# 本机构建辅助文件（含绝对路径，仅本地使用）
backend/settings-workspace.xml

# 迁移期权限探测残留文件（无法删除时忽略，不影响构建）
__probe_write.txt
**/__probe_write.txt
.__probe_write.txt
